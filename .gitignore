java/target/
rust/target/

# App and service log files
*.log

# Local observability secrets (Grafana admin password)
.env.observability

# Optional app log overrides (see .env.apps.example)
.env.apps
