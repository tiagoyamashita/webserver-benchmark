# Docker image: install these before copying src/ so dependency layers cache across code edits.
# Local venv: use requirements-dev.txt via scripts/init_venv.* instead.
flask>=3.0
prometheus-client>=0.21
psycopg[binary]>=3.2
redis>=5.0
kafka-python>=2.0
pytest>=8
