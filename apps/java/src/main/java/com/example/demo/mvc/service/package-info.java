/**
 * Application service layer: business rules and orchestration between web controllers and
 * persistence. Use {@link org.springframework.stereotype.Service} types; keep controllers thin.
 *
 * <p>Typical flow: {@code mvc.web} → {@code mvc.service} → {@code mvc.persistence}.
 */
package com.example.demo.mvc.service;
