# DevTools RestartClassLoader only reloads project classes by default; third-party
# jars (e.g. jbcrypt) must be explicitly included or password hashing fails at runtime.
restart.include.jbcrypt=/jbcrypt-[\\w\\.-]+\\.jar
