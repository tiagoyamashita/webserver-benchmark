-- Users (com.example.demo.exercises.db.User). Portable H2 + PostgreSQL.
CREATE TABLE users (
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name       VARCHAR(255) NOT NULL,
    email      VARCHAR(255) NOT NULL,
    created_at TIMESTAMP(6) NOT NULL
);
