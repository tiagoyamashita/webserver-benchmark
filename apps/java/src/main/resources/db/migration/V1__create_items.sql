-- Items API (com.example.demo.exercises.db.Item). Portable H2 + PostgreSQL.
CREATE TABLE items (
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name       VARCHAR(255) NOT NULL,
    created_at TIMESTAMP(6) NOT NULL
);
