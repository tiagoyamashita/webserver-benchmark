# Podman: from repo root — podman compose up --build   (or: docker compose up --build)
# Starts postgres + java + python + rust. ELK/Grafana live under elk/ and grafana/.
services:
  postgres:
    image: docker.io/library/postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: demo
    ports:
      - "5432:5432"
    volumes:
      - exercises_pg_data:/var/lib/postgresql/data

  java:
    build:
      context: ./java
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: postgres
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: demo
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
    depends_on:
      - postgres

  python:
    build:
      context: ./python
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      FLASK_RUN_HOST: "0.0.0.0"
      FLASK_RUN_PORT: "5000"
      EXERCISES_PROJECT_ROOT: /app

  rust:
    build:
      context: ./rust
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    environment:
      EXERCISES_WEB_HOST: "0.0.0.0"
      EXERCISES_WEB_PORT: "8082"
      EXERCISES_RUST_ROOT: /app

volumes:
  exercises_pg_data:
