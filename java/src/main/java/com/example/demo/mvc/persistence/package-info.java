/**
 * Persistence layer: JPA entities, Spring Data repositories, and database-facing types.
 *
 * <p>Entities map to tables; repositories extend {@link org.springframework.data.jpa.repository.JpaRepository}
 * (or similar). Align packages with exercise tracks (e.g. DB exercises vs controller-only demos).
 */
package com.example.demo.mvc.persistence;
